package com.rpg.entity

class EntityHandler {

  //TODO thread entity handler
  //list of entities and goes through a list telling entities to update themselves (updating on their own threads)
      //only update things that are outside the player's peripheral or inside it
      //pass to each entity the location of the player and let it decide if it needs to update

}
